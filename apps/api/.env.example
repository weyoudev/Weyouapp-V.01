# Copy to .env and set values. Root .env is also used when running from repo root.
PORT=3000
# Use your Supabase project: Project settings → Database. Prefer "Connection pooling" (port 6543) if direct (5432) is unreachable.
DATABASE_URL=postgresql://postgres.PROJECT_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
JWT_SECRET="dev-secret"

# Storage (optional for Step 7)
STORAGE_DRIVER=local
LOCAL_STORAGE_ROOT=./storage

# Twilio Verify (customer OTP). Without these, backend accepts fixed OTP 123456 for dev.
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_VERIFY_SERVICE_SID=
